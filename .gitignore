# Dependencies
node_modules/
.pnpm-store/

# Изолированный тулинг для оптимизации ассетов (scripts/optimize-hero.mjs)
.tooling/

# Build output (сервер собирает сам через pnpm build)
dist/
*.tsbuildinfo

# Локальный прод-архив (артефакт сборки, не версионируем)
trofimoff-tdistina-production.zip

# Editor / OS
.DS_Store
Thumbs.db
*.log
~$*
